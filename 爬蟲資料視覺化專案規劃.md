# 爬蟲 → 清洗 → API → 視覺化 專案規劃筆記

## 一、整體架構

```
requests (抓取)
   ↓
bs4 (解析 HTML)
   ↓
pandas (清洗 / 轉換)
   ↓
[資料儲存層 — 視需求決定是否要資料庫]
   ↓
FastAPI (包成 API)
   ↓
ECharts (前端畫面呈現)
```

這個流程屬於「資料工程 + 視覺化呈現」(data pipeline + dashboard),
如果要更貼近「數據分析」,建議在 pandas 清洗完之後,加入一層統計描述
或簡單模型(平均、趨勢、相關性、迴歸/預測),再讓 ECharts 呈現「分析後的結果」。

---

## 二、各階段重點與常見坑

### 1. requests 抓取資料
- 注意 User-Agent、Headers 偽裝,避免被辨識為機器人
- 分頁抓取要設計重試機制(timeout、retry、間隔時間避免被封鎖)
- 留意目標網站是否有 robots.txt 限制,尊重使用條款
- 若網站是 JS 動態渲染,requests 抓不到內容,需改用 Selenium / Playwright

### 2. bs4 解析資料
- 網站改版會讓 selector 失效,解析邏輯要寫得有彈性(用多個備援 selector)
- 注意編碼問題(utf-8 / big5 混用時常見亂碼)
- 巢狀標籤結構複雜時,建議先用瀏覽器開發者工具定位好 CSS selector 或 XPath 邏輯

### 3. pandas 清洗
- 常見陷阱:欄位型別轉換(字串 "1,000" 轉數字、日期格式不一致)
- 缺值處理策略要想清楚(補值、刪除、標記)
- 資料去重、欄位命名一致化
- 大量資料時注意記憶體用量,必要時分批處理(chunk)

### 4. FastAPI 包資料
- 設計清楚的 API 路由與回傳格式(建議用 Pydantic model 定義 schema)
- 加上基本的 CORS 設定,否則前端 fetch 會被瀏覽器擋
- 是否需要快取(cache)視資料更新頻率而定,避免每次請求都重新爬取/清洗

### 5. ECharts 前端呈現
- 前端串接 API 時處理好 loading / error 狀態
- 圖表類型挑選要符合資料性質(趨勢用折線圖、比較用長條圖、佔比用圓餅圖)
- 資料量大時考慮前端分頁或後端先做聚合再傳給前端,避免瀏覽器卡頓

---

## 三、時間估算(依熟練度抓大概值)

| 階段 | 時間估算(熟練情況下) |
|---|---|
| requests 爬蟲(含試錯、處理反爬、分頁) | 半天~1天 |
| pandas 清洗 | 幾小時~半天 |
| FastAPI 包 API | 半天 |
| 前端 ECharts 串接 | 半天~1天 |
| **整體 MVP** | **2~4 天**(資料源單純可能一天內跑通) |

若資料源反爬機制強、資料很髒、或前端要做多維度互動篩選,時間可能拉長到一週左右。

---

## 四、需不需要資料庫?

**結論:這個流程本身「不一定需要」資料庫,但要看幾個關鍵條件。**

### 可以不用資料庫的情況
- 資料是「一次性」或「低頻更新」(例如每天跑一次爬蟲、更新一次)
- 資料量不大,csv / parquet / json 檔案就能負荷
- 不需要多人同時寫入、不需要複雜查詢或關聯
- 做法:pandas 清洗完直接存成檔案(如 `data.parquet` 或 `data.csv`),
  FastAPI 啟動時讀進記憶體,或每次請求時讀檔案回傳。這樣架構最簡單,
  開發速度最快。

### 建議加上資料庫的情況
- 需要保留「歷史資料」做趨勢比對(例如每天爬一次,想看時間序列變化)
- 資料量會持續增長,檔案讀寫效能會變差
- 需要多條件查詢、篩選、聚合(SQL 的 WHERE / GROUP BY 會比在 pandas 裡重複處理更有效率)
- 未來可能有多個服務或多人同時存取這份資料
- 做法:
  - 小型專案 / 個人專案:**SQLite** 就很夠用,零設定、檔案型資料庫
  - 需要正式部署 / 之後可能擴充:**PostgreSQL** 或 **MySQL**
  - 若資料是時間序列型態(定期爬取存歷史),可以考慮 **TimescaleDB**(PostgreSQL 擴充)

### 折衷做法
先不用資料庫,用檔案儲存把 MVP 做出來;等真的有「歷史資料需求」或
「效能瓶頸」出現時,再把儲存層換成 SQLite/PostgreSQL。FastAPI 的
好處是儲存層抽換相對容易,不會綁死你前期的架構選擇。

---

## 五、後續優化方向(讓專案更像「數據分析」)

1. 在清洗完的資料上加入基本統計描述(平均、中位數、標準差、趨勢)
2. 用簡單模型做預測或分群(線性迴歸、移動平均、K-means)
3. ECharts 呈現「分析後的洞察」而不只是原始數據攤開
4. 加上排程工具(如 `APScheduler` 或 cron)讓爬蟲自動定期執行,累積歷史資料
