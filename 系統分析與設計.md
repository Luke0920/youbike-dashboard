# YouBike 台北市車況即時儀表板 — 系統分析與設計

> 專案期間:2026/08/27 ～ 2026/09/16(約 20 天)
> 目標:完成一個「爬蟲 → 清洗 → 排程累積 → API → 視覺化」的完整資料專案,並可公開展示,做為面試作品集(資料分析 / 資料工程 / 前端視覺化 三個方向皆可講)。

---

## 目錄

1. [專案概述](#一專案概述)
2. [系統架構](#二系統架構)
3. [資料來源分析](#三資料來源分析)
4. [技術選型與套件清單](#四技術選型與套件清單)
5. [資料庫設計](#五資料庫設計)
6. [檔案結構與各檔案職責](#六檔案結構與各檔案職責)
7. [API 設計](#七api-設計)
8. [功能 ↔ 檔案 ↔ 資料 對應表](#八功能--檔案--資料-對應表)
9. [關鍵實作方法](#九關鍵實作方法)
10. [每日開發計畫](#十每日開發計畫)
11. [風險與降級方案](#十一風險與降級方案)
12. [面試準備重點](#十二面試準備重點)

---

## 一、專案概述

### 1.1 一句話定位

> 每 10 分鐘抓取台北市 YouBike 各站即時車況,累積成時間序列資料,分析「通勤潮汐、尖離峰缺車、站點類型」,並以互動地圖與圖表呈現。

### 1.2 核心觀念

官方 API **只提供「當下這一秒」的快照**,沒有歷史資料。
本專案的價值來自「**自己排程定時抓取、累積歷史**」,所有分析與圖表都建立在這份累積資料上。
→ 因此「排程抓取 + 寫入資料庫」必須在專案第一週就跑起來,越早開始累積,展示時資料越厚。

### 1.3 「即時」的真正含義(難度說明)

「即時」在本專案**不是** WebSocket 推播 / 串流,只是兩件單純的事:

| 動作 | 實作方式 | 難度 |
|---|---|---|
| 後端定時抓取 | GitHub Actions cron 每 10 分鐘跑一次抓取腳本 | 低 |
| 前端定時刷新 | `setInterval` 每 60 秒重新 `fetch` 一次並重畫地圖 | 低 |

真正花時間的是:地圖 geoJSON 串接、查詢效能、部署設定,而不是「即時」本身。

---

## 二、系統架構

```
┌─────────────────────────────────────────────────────────────┐
│  官方即時 JSON API                                            │
│  https://tcgbusfs.blob.core.windows.net/dotapp/youbike/v2/    │
│  youbike_immediate.json                                       │
└───────────────┬─────────────────────────────┬───────────────┘
                │                             │
     (每 10 分鐘,排程)                (前端每 60 秒,即時地圖)
                │                             │
                ▼                             │
   ┌────────────────────────┐                 │
   │ ingest/  抓取 + 清洗    │                 │
   │  fetcher → transform    │                 │
   │        → store          │                 │
   └───────────┬────────────┘                 │
               ▼                              │
   ┌────────────────────────┐                 │
   │  資料庫 (Postgres/SQLite)│                │
   │  ├ station     靜態站點  │                │
   │  ├ snapshot    歷史快照  │                │
   │  └ station_hourly 每小時聚合│             │
   └───────────┬────────────┘                 │
               ▼                              │
   ┌────────────────────────┐                 │
   │  analysis/ 分析運算      │                │
   │  aggregate / peak /      │                │
   │  ranking / flow / cluster│                │
   └───────────┬────────────┘                 │
               ▼                              │
   ┌────────────────────────┐                 │
   │  app/  FastAPI          │                 │
   │  /stations/live         │◀────────────────┘ (可選:後端代理即時資料)
   │  /stations/{sno}/history│
   │  /stats/peak-heatmap    │
   │  /stats/ranking         │
   │  /stats/commute-flow    │
   └───────────┬────────────┘
               ▼
   ┌────────────────────────┐
   │  frontend/ ECharts      │
   │  即時地圖 / KPI 卡       │
   │  尖離峰熱力圖 / 排行榜   │
   │  單站歷史折線           │
   └────────────────────────┘
```

### 部署拓撲

| 元件 | 平台(免費方案) | 說明 |
|---|---|---|
| 歷史累積排程 | GitHub Actions(cron `*/10 * * * *`) | 不會休眠,是可靠的資料累積來源 |
| 資料庫 | Neon 或 Supabase 免費 PostgreSQL | Actions 與 API 共用同一個 DB;本地開發用 SQLite |
| API 後端 | Render / Railway 免費方案 | 讀資料庫、提供分析 endpoint |
| 前端 | Vercel / Netlify / GitHub Pages | 靜態網頁,即時地圖直接打官方 JSON |

> 為什麼即時地圖讓前端直接打官方 JSON:官方這支是 Azure Blob 靜態檔,通常允許瀏覽器跨域抓取,且免經過會休眠的後端,即時性最穩。若官方 CORS 被擋,再改走後端 `/stations/live` 代理。

---

## 三、資料來源分析

### 3.1 主要資料源

- **台北市 YouBike 2.0 即時資訊**
  `https://tcgbusfs.blob.core.windows.net/dotapp/youbike/v2/youbike_immediate.json`
  格式:JSON 陣列,每個站一個物件;約 1,300+ 站;每分鐘更新。

### 3.2 欄位對應(以實際 API 回傳為準,Day 1 要先打一次確認)

官方曾改過欄位命名,可能是下列其一,程式裡用 `transform.py` 統一改成專案內部名稱:

| 專案內部欄位 | 官方可能欄位名 A | 官方可能欄位名 B | 說明 |
|---|---|---|---|
| `sno` | `sno` | `station_no` | 站點代號(主鍵) |
| `sna` | `sna` | `station_name` | 站名(常帶「YouBike2.0_」前綴,清洗時去掉) |
| `sarea` | `sarea` | `district` | 行政區 |
| `lat` | `lat` / `latitude` | `latitude` | 緯度 |
| `lng` | `lng` / `longitude` | `longitude` | 經度 |
| `addr` | `ar` | `address` | 地址 |
| `tot` | `tot` | `total` | 總停車格 |
| `sbi` | `sbi` | `available_rent_bikes` | **可借車輛數** |
| `bemp` | `bemp` | `available_return_bikes` | **可還空位數** |
| `act` | `act` | `act` | 是否啟用(0/1) |
| `updated_at` | `mday` / `infoTime` / `srcUpdateTime` | `updateTime` | 資料更新時間(台北時間字串) |

### 3.3 進階資料源(有時間再接)

- **中央氣象署開放資料**(降雨、溫度):做「天氣 vs 使用量」關聯分析。需申請免費 API key。
- **新北市 YouBike**(`data.ntpc.gov.tw`):擴大涵蓋範圍。格式與台北略不同,`transform.py` 要多一組對應。

### 3.4 法遵注意

- 官方開放資料,採 CC BY 4.0 授權,可自由使用,README 註明出處即可。
- 抓取頻率 10 分鐘/次,遠低於任何合理限制,不構成濫用。

---

## 四、技術選型與套件清單

### 4.1 後端 / 資料層(Python 3.11+)

| 套件 | 用途 | 必要性 |
|---|---|---|
| `requests` | 抓取官方 JSON | MVP 必要 |
| `tenacity` | 抓取失敗自動重試(指數退避) | MVP 必要 |
| `pandas` | 清洗、聚合、分析運算 | MVP 必要 |
| `SQLAlchemy` | ORM / 資料庫抽象層(讓 SQLite ↔ Postgres 可切換) | MVP 必要 |
| `psycopg2-binary` | PostgreSQL 驅動(正式環境) | 部署時必要 |
| `APScheduler` | 本地長駐排程(備援方案) | 建議 |
| `fastapi` | API 框架 | MVP 必要 |
| `uvicorn[standard]` | ASGI 伺服器 | MVP 必要 |
| `pydantic` | 回傳資料 schema(FastAPI 內建 v2) | MVP 必要 |
| `pydantic-settings` | 讀 `.env` 設定 | MVP 必要 |
| `scikit-learn` | K-means 站點分群 | 進階 |
| `pytest` | 單元測試 | 建議 |
| `httpx` | 測試 FastAPI(TestClient 相依) | 建議 |
| `ruff` | Lint + 格式化 | 建議 |

`requirements.txt`:

```
requests
tenacity
pandas
SQLAlchemy
psycopg2-binary
APScheduler
fastapi
uvicorn[standard]
pydantic-settings
scikit-learn      # 進階(分群)才需要
pytest
httpx
ruff
```

### 4.2 前端

| 技術 | 用途 | 備註 |
|---|---|---|
| 純 HTML + CSS + JavaScript(ES modules) | 頁面結構與邏輯 | 不用框架,保持簡單 |
| `echarts`(v5) | 所有圖表與地圖 | 用 CDN 或下載 `echarts.min.js` 放本地 |
| 台北市行政區 GeoJSON | 地圖底圖 | 搜尋「台北市 行政區 geojson」(g0v / 政府開放資料),放 `frontend/geo/` |

> 若之後想用框架:Vite + Vue 3。但本專案時程建議先用 vanilla JS。

### 4.3 開發與部署工具

- `venv`(或 `uv`)建虛擬環境
- `git` + GitHub(含 GitHub Actions)
- `Docker` + `docker-compose`(可選,加分,面試可講)
- 部署:Render/Railway(後端)、Vercel/Pages(前端)、Neon/Supabase(資料庫)

---

## 五、資料庫設計

### 5.1 `station` — 靜態站點資料(偶爾更新)

| 欄位 | 型別 | 說明 |
|---|---|---|
| `sno` | TEXT | 主鍵,站點代號 |
| `sna` | TEXT | 站名(已去前綴) |
| `sarea` | TEXT | 行政區 |
| `lat` | REAL | 緯度 |
| `lng` | REAL | 經度 |
| `addr` | TEXT | 地址 |
| `tot` | INTEGER | 總停車格 |
| `updated_at` | DATETIME | 本列最後更新時間 |

寫入方式:**upsert**(`INSERT ... ON CONFLICT(sno) DO UPDATE`)。

### 5.2 `snapshot` — 歷史快照(每站每次抓取一列)

| 欄位 | 型別 | 說明 |
|---|---|---|
| `id` | INTEGER | 自增主鍵 |
| `sno` | TEXT | 外鍵 → station.sno |
| `ts` | DATETIME | 資料時間(用官方更新時間,統一時區) |
| `sbi` | INTEGER | 可借車數 |
| `bemp` | INTEGER | 可還空位數 |
| `act` | INTEGER | 是否啟用 |

- 索引:`INDEX (sno, ts)`
- 唯一鍵:`UNIQUE (sno, ts)` → 讓重複執行不會插入重複資料(idempotent)
- 寫入方式:`INSERT OR IGNORE` / `ON CONFLICT DO NOTHING`
- 資料量估算:1,300 站 × 每 10 分鐘 = 每天約 18.7 萬列、每月約 560 萬列。Postgres/SQLite 都可負荷,但**查詢一定要走索引**,歷史圖表改查下面的聚合表。

### 5.3 `station_hourly` — 每小時聚合表(由 `analysis/aggregate.py` 產生)

| 欄位 | 型別 | 說明 |
|---|---|---|
| `sno` | TEXT | 站點代號 |
| `hour_bucket` | DATETIME | 整點時間(該小時起點) |
| `avg_sbi` | REAL | 該小時平均可借車數 |
| `avg_fill_rate` | REAL | 該小時平均滿位率(sbi/tot) |
| `empty_ratio` | REAL | 該小時內 sbi==0(借不到車)的比例 |
| `full_ratio` | REAL | 該小時內 bemp==0(還不了車)的比例 |
| `sample_count` | INTEGER | 該小時的樣本數(資料完整度檢查) |

- 主鍵:`(sno, hour_bucket)`
- 前端所有「歷史 / 尖離峰 / 排行」圖表都查這張表,不查 `snapshot`(近 7 天內的單站細節折線除外)。

---

## 六、檔案結構與各檔案職責

```
youbike-dashboard/
├── README.md                     專案說明:架構圖、截圖、如何執行、已知限制、下一步
├── requirements.txt              Python 套件清單
├── .env.example                  環境變數範本(DATABASE_URL、FETCH_INTERVAL_MIN 等)
├── .gitignore                    忽略 venv/、*.db、.env
├── config.py                     讀取環境變數,集中管理設定(API URL、DB 連線、抓取間隔)
│
├── ingest/                       【資料抓取層】
│   ├── __init__.py
│   ├── fetcher.py                抓官方 JSON:requests + timeout + tenacity 重試,回傳原始 list
│   ├── transform.py              清洗:官方欄位 → 內部欄位改名、型別轉換、去站名前綴、過濾 act=0、經緯度合法性檢查。回傳 (stations, snapshots) 兩份乾淨資料
│   ├── store.py                  寫入 DB:station 做 upsert;snapshot 做 insert-or-ignore
│   └── run_once.py               把 fetch → transform → store 串成一次完整流程。可獨立執行(CLI)。排程與 GitHub Actions 都呼叫這支。並印出/記錄「這次抓到幾站」供監控
│
├── scheduler.py                  本地長駐排程:APScheduler 每 N 分鐘呼叫 run_once(備援方案,主力是 GitHub Actions)
│
├── db/                           【資料庫層】
│   ├── __init__.py
│   ├── models.py                 SQLAlchemy models:Station、Snapshot、StationHourly
│   ├── session.py                建立 engine 與 SessionLocal,依 DATABASE_URL 切換 SQLite/Postgres
│   └── init_db.py                建立所有資料表(第一次執行時跑一次)
│
├── analysis/                     【分析運算層】
│   ├── __init__.py
│   ├── aggregate.py              snapshot → station_hourly(每小時平均可借車、滿位率、缺車/爆滿比例)。排程每小時跑一次
│   ├── peak.py                   尖離峰:依「小時 × 星期別(平日/假日)」聚合各站/各區的平均缺車率,產生熱力圖資料
│   ├── ranking.py                排行榜:最常借不到車 Top N、最常還不了車 Top N(用 empty_ratio / full_ratio 排序)
│   ├── flow.py                   【進階】借還流量推估:相鄰快照 sbi 差值 → 淨流入/流出;找出通勤「淨流出站(住宅型)」與「淨流入站(辦公型)」
│   └── cluster.py                【進階】站點分群:用各站 24 小時日內曲線做 K-means,分成住宅型/辦公型/觀光型/轉運型
│
├── app/                          【API 層】
│   ├── __init__.py
│   ├── main.py                   FastAPI 實例、CORS 設定、掛載路由;lifespan 內可選擇性啟動 APScheduler
│   ├── schemas.py                Pydantic 回傳模型(StationLive、HistoryPoint、HeatmapCell、RankingItem...)
│   ├── deps.py                   依賴注入:提供 DB session
│   ├── cache.py                  簡單 TTL 記憶體快取({key: (data, expire_ts)}),包在 stats endpoint 上
│   └── routers/
│       ├── __init__.py
│       ├── stations.py           /stations/live、/stations/{sno}/history
│       └── stats.py              /stats/peak-heatmap、/stats/ranking、/stats/commute-flow、/stats/clusters
│
├── frontend/                     【前端視覺化層】
│   ├── index.html                版面:頂部 KPI 卡列、左側地圖、右側圖表區
│   ├── style.css                 樣式、RWD、loading/error 狀態
│   ├── geo/
│   │   └── taipei-districts.geojson   台北市 12 行政區地圖
│   └── js/
│       ├── config.js             前端設定:API base URL、官方即時 JSON URL、刷新間隔
│       ├── api.js                包裝 fetch:錯誤處理、逾時、回傳 JSON
│       ├── map.js                即時地圖:echarts.registerMap + geo 底圖 + scatter 站點;顏色=滿位率,大小=容量;hover tooltip
│       ├── kpi.js                KPI 卡:全市即時可借車總數、啟用站數、目前缺車站數、目前爆滿站數
│       ├── heatmap.js            尖離峰熱力圖:x=24 小時,y=行政區,色深=平均缺車率
│       ├── ranking.js            缺車 / 爆滿排行榜表格
│       ├── station-detail.js     點地圖站點 → 呼叫 history API → 畫過去 7 天可借車折線
│       └── main.js               初始化所有模組、setInterval 定時更新即時區塊、visibilitychange 時暫停
│
├── tests/                        【測試】
│   ├── __init__.py
│   ├── conftest.py               pytest fixture:記憶體 SQLite、樣本資料
│   ├── test_transform.py         測清洗:欄位改名、型別轉換、髒資料過濾
│   ├── test_store.py             測寫入:upsert 不重複、insert-or-ignore idempotent
│   └── test_api.py               測 API:各 endpoint 回傳格式與狀態碼(FastAPI TestClient)
│
├── .github/workflows/
│   ├── ci.yml                    每次 push:跑 ruff + pytest
│   └── ingest.yml               排程 cron `*/10 * * * *`:安裝相依 → 執行 run_once.py(寫入雲端 Postgres)
│
├── Dockerfile                    (可選)打包後端
└── docker-compose.yml           (可選)後端 + 本地 Postgres,方便本機開發
```

---

## 七、API 設計

所有回傳皆為 JSON。時間一律用 ISO 8601(+08:00)。

| Method | Path | 用途 | Query 參數 | 資料來源 | 快取 |
|---|---|---|---|---|---|
| GET | `/stations/live` | 全站目前車況(即時地圖用;前端主要直接打官方,這支作為備援/代理) | `sarea`(可選,篩行政區) | 官方 API 或 snapshot 最新一筆 | 30 秒 |
| GET | `/stations/{sno}/history` | 單站過去 N 天可借車數變化 | `days`(預設 7) | snapshot | 5 分 |
| GET | `/stats/peak-heatmap` | 尖離峰熱力圖資料(小時 × 行政區 → 平均缺車率) | `daytype`(weekday/weekend) | station_hourly | 15 分 |
| GET | `/stats/ranking` | 缺車 / 爆滿站排行 | `metric`(empty/full)、`limit`(預設 10) | station_hourly | 15 分 |
| GET | `/stats/commute-flow` | 【進階】通勤淨流入/流出站分類 | `hour`(預設 8) | snapshot | 15 分 |
| GET | `/stats/clusters` | 【進階】站點分群結果 + 各群代表曲線 | 無 | station_hourly | 60 分 |
| GET | `/health` | 健康檢查(部署平台用) | 無 | — | 無 |

回傳範例(`/stats/ranking?metric=empty&limit=5`):

```json
{
  "metric": "empty",
  "generated_at": "2026-09-10T14:00:00+08:00",
  "items": [
    {"sno": "500101001", "sna": "捷運科技大樓站", "sarea": "大安區", "empty_ratio": 0.42, "peak_hour": 8},
    ...
  ]
}
```

---

## 八、功能 ↔ 檔案 ↔ 資料 對應表

| 功能 | 前端檔案 | API endpoint | 後端分析/資料檔案 | 使用的資料表 | MVP? |
|---|---|---|---|---|---|
| 即時地圖(站點顏色=滿位率) | `js/map.js` | 官方 JSON(或 `/stations/live`) | `ingest/store.py` 持續寫入 | `snapshot`(最新)、`station` | ✅ |
| KPI 卡(即時統計數字) | `js/kpi.js` | 同上一份資料計算 | — | `snapshot`(最新) | ✅ |
| 單站歷史折線(過去 7 天) | `js/station-detail.js` | `/stations/{sno}/history` | 直接查詢 | `snapshot` | ✅ |
| 尖離峰熱力圖 | `js/heatmap.js` | `/stats/peak-heatmap` | `analysis/aggregate.py` + `analysis/peak.py` | `station_hourly` | ✅ |
| 缺車 / 爆滿排行榜 | `js/ranking.js` | `/stats/ranking` | `analysis/ranking.py` | `station_hourly` | ✅ |
| 通勤潮汐(淨流入/流出) | `js/flow.js`(新增) | `/stats/commute-flow` | `analysis/flow.py` | `snapshot` | ⬆ 進階 |
| 站點分群 | `js/cluster.js`(新增) | `/stats/clusters` | `analysis/cluster.py` | `station_hourly` | ⬆ 進階 |
| 天氣關聯分析 | 圖表區塊 | (併入 stats) | `analysis/` 新增 + 氣象 API | `snapshot` + 氣象資料 | ⬆ 進階 |

**資料流向(以尖離峰熱力圖為例):**

```
GitHub Actions 每 10 分
  → ingest/run_once.py
      → fetcher.py 抓官方 JSON
      → transform.py 清洗
      → store.py 寫入 snapshot
排程每小時
  → analysis/aggregate.py：讀 snapshot → 算每小時統計 → 寫 station_hourly
使用者開啟網頁
  → js/heatmap.js 呼叫 GET /stats/peak-heatmap
      → app/routers/stats.py（先查 cache）
          → analysis/peak.py：讀 station_hourly → 依小時×行政區聚合
      → 回傳 JSON
  → ECharts 畫熱力圖
```

---

## 九、關鍵實作方法

### 9.1 抓取(`ingest/fetcher.py`)

- `requests.get(url, timeout=10, headers={"User-Agent": "..."})`
- 用 `tenacity` 裝飾器:重試 3 次、指數退避(2s → 4s → 8s)、只對連線錯誤/5xx 重試
- 回傳解析後的 `list[dict]`;抓不到就丟例外,讓上層記錄失敗

### 9.2 清洗(`ingest/transform.py`)

- 官方欄位名 → 內部欄位名(用一個 `FIELD_MAP` dict,對應多種可能名稱)
- 型別:`sbi/bemp/tot/act` 轉 int;`lat/lng` 轉 float
- 站名去掉 `YouBike2.0_` 前綴
- 過濾:`act == 0`(停用站)、經緯度不在台北合理範圍者
- 時間:官方時間字串 → `datetime`,統一為 `Asia/Taipei` 時區
- 回傳 `(stations: list[dict], snapshots: list[dict])`

### 9.3 寫入(`ingest/store.py`)

- `station`:SQLAlchemy 的 `insert(...).on_conflict_do_update(index_elements=["sno"], set_=...)`
- `snapshot`:`insert(...).on_conflict_do_nothing(index_elements=["sno", "ts"])`
  → GitHub Actions 與本地排程即使同時跑,也不會產生重複資料(idempotent)

### 9.4 排程(兩層,互為備援)

| 層 | 檔案 | 說明 |
|---|---|---|
| 主力 | `.github/workflows/ingest.yml` | cron `*/10 * * * *`;不會休眠;寫入雲端 Postgres。注意 GitHub Actions 排程尖峰時可能延遲數分鐘,可接受 |
| 備援 | `scheduler.py`(APScheduler) | 本地或部署後端內執行;若後端平台會休眠則不可靠,故只當補強 |

### 9.5 聚合(`analysis/aggregate.py`)

- 每小時執行一次(GitHub Actions 另一個 workflow,或後端排程)
- 讀「上一個完整小時」的 `snapshot`,`groupby(sno, hour)` 算 `avg_sbi`、`avg_fill_rate`、`empty_ratio`、`full_ratio`、`sample_count`
- upsert 進 `station_hourly`

### 9.6 借還流量推估(`analysis/flow.py`,進階)

- 對單一站,把 `snapshot` 依 `ts` 排序,計算相鄰兩筆的 `sbi` 差
- 差為負 → 借車事件(數量 = |差|);差為正 → 還車事件
- 依小時彙總每站的「借出總數 − 還入總數」= 淨流出量
- 早上 7–9 點淨流出大的站 → 住宅型;淨流入大的站 → 辦公型
- 注意:調度車輛補貨也會造成 sbi 跳升,屬雜訊,報告中說明此限制

### 9.7 前端即時地圖(`frontend/js/map.js`)

- `fetch` 官方 JSON(或 `/stations/live`)
- `echarts.registerMap('taipei', taipeiGeoJson)`
- `geo` component 畫行政區底圖;`series` 用 `scatter`(`coordinateSystem: 'geo'`),`data` 為 `[lng, lat, 額外欄位]`
- `visualMap` 把滿位率映射到顏色(紅→黃→綠)
- `symbolSize` 依 `tot` 決定
- tooltip 顯示站名、可借、可還、更新時間
- 點擊站點 → 觸發 `station-detail.js` 載入該站歷史折線

### 9.8 前端定時刷新(`frontend/js/main.js`)

- `setInterval(refreshLive, 60_000)`
- `document.addEventListener('visibilitychange', ...)`:分頁隱藏時 `clearInterval`,回來再啟動
- 每次刷新只更新地圖 series data 與 KPI,不重建整張圖

### 9.9 快取(`app/cache.py`)

- 極簡:模組級 dict `{key: (payload, expire_epoch)}`
- `get_or_set(key, ttl_seconds, compute_fn)`
- stats endpoint 包 15 分鐘;live endpoint 包 30 秒

### 9.10 CORS(`app/main.py`)

- `CORSMiddleware`,`allow_origins` 填前端正式網域 + `http://localhost:5173`(本地)
- `allow_methods=["GET"]`

### 9.11 設定管理(`config.py`)

- `pydantic-settings` 的 `BaseSettings`
- 欄位:`database_url`、`youbike_api_url`、`fetch_interval_min`、`cors_origins`、`cwb_api_key`(進階)
- 本地讀 `.env`;部署平台用環境變數面板設定

---

## 十、每日開發計畫

> 假設每天可投入 2–4 小時,週末較多。粗體為「當天不能不做完」的關鍵項。
> 進度落後時,優先砍「進階」項目,確保 MVP + 部署完成。

### 第 0 週:資料管線先跑起來(8/27 三 ～ 8/29 五)

| 日期 | 階段 | 具體工作 | 產出檔案 | 完成標準 |
|---|---|---|---|---|
| 8/27(三) | 環境 + DB | 建 GitHub repo、venv、`requirements.txt`、`.gitignore`、`config.py`、`.env.example`。**實際用瀏覽器/curl 打一次官方 API,記錄真實欄位名**。寫 `db/models.py`、`db/session.py`、`db/init_db.py`,建表成功 | `config.py`、`db/*` | `python -m db.init_db` 能在本地 SQLite 建出 3 張表 |
| 8/28(四) | 抓取 + 清洗 | 寫 `ingest/fetcher.py`(timeout + tenacity)、`ingest/transform.py`(欄位對應、型別、過濾)。寫 `tests/test_transform.py` | `ingest/fetcher.py`、`ingest/transform.py`、`tests/test_transform.py` | `pytest tests/test_transform.py` 全綠;手動呼叫 fetcher 能取回 1,000+ 站 |
| 8/29(五) | 寫入 + 串接 | 寫 `ingest/store.py`(upsert / insert-or-ignore)、`ingest/run_once.py`。本地跑 `python -m ingest.run_once` 數次,確認資料進 DB 且不重複。寫 `scheduler.py` | `ingest/store.py`、`ingest/run_once.py`、`scheduler.py` | **`run_once` 連跑兩次,snapshot 不會出現重複 (sno, ts);station 有 ~1,300 列** |

### 第 1 週:雲端累積 + API + 即時前端 + 部署(8/30 六 ～ 9/5 五)

| 日期 | 階段 | 具體工作 | 產出檔案 | 完成標準 |
|---|---|---|---|---|
| 8/30(六) | 雲端排程 | 開 Neon/Supabase 免費 Postgres。寫 `.github/workflows/ingest.yml`(cron `*/10 * * * *` 跑 `run_once`,`DATABASE_URL` 放 GitHub Secrets)。**push 後確認 Actions 有成功寫入雲端 DB** | `.github/workflows/ingest.yml` | Actions 執行成功,雲端 DB 每 10 分鐘增加約 1,300 列 snapshot |
| 8/31(日) | API 骨架 | 寫 `app/main.py`(FastAPI + CORS)、`app/schemas.py`、`app/deps.py`、`app/routers/stations.py` 的 `/stations/live`(讀最新 snapshot)。本地 `uvicorn` 測 `/docs` | `app/main.py`、`app/schemas.py`、`app/deps.py`、`app/routers/stations.py` | `/stations/live` 在本地回傳全站 JSON;`/health` 回 200 |
| 9/1(一) | 即時地圖 | 下載台北行政區 geojson。寫 `frontend/index.html`、`style.css`、`js/config.js`、`js/api.js`、`js/map.js`。地圖能畫出底圖 + 站點散點 + 顏色 | `frontend/index.html`、`frontend/style.css`、`frontend/geo/*`、`frontend/js/{config,api,map}.js` | 本地開網頁能看到台北地圖與上千個彩色站點,hover 有 tooltip |
| 9/2(二) | KPI + 刷新 | 寫 `js/kpi.js`、`js/main.js`(`setInterval` 60 秒)。整體即時畫面完成 | `frontend/js/{kpi,main}.js` | 開著網頁不動,60 秒後地圖與 KPI 數字會自動更新 |
| 9/3(三) | 部署 | 後端上 Render/Railway(設 `DATABASE_URL`、`CORS_ORIGINS`);前端上 Vercel/Pages。設定前端 `config.js` 指向線上 API | `Dockerfile`(可選)、`README` 部署段 | **有一個公開網址能看到會動的即時地圖** |
| 9/4(四) | 緩衝 | 修部署問題:CORS、HTTPS mixed content、free tier 冷啟動、geojson 路徑。整理 commit | — | 線上版穩定;README 有「如何本地執行」段落 |
| 9/5(五) | 聚合 | 寫 `analysis/aggregate.py`(snapshot → station_hourly)。加一個 GitHub Actions workflow 每小時跑聚合,或併進 ingest 流程 | `analysis/aggregate.py`、`.github/workflows/aggregate.yml` | `station_hourly` 開始有資料;抽查數字合理 |

### 第 2 週:歷史分析與圖表(9/6 六 ～ 9/12 五)

| 日期 | 階段 | 具體工作 | 產出檔案 | 完成標準 |
|---|---|---|---|---|
| 9/6(六) | 尖離峰 API | 寫 `analysis/peak.py` + `app/routers/stats.py` 的 `/stats/peak-heatmap` | `analysis/peak.py`、`app/routers/stats.py` | endpoint 回傳 24×行政區 的缺車率矩陣 |
| 9/7(日) | 尖離峰前端 | 寫 `frontend/js/heatmap.js`,ECharts heatmap 串接 | `frontend/js/heatmap.js` | 網頁顯示熱力圖,看得出上下班尖峰帶 |
| 9/8(一) | 排行榜 | 寫 `analysis/ranking.py` + `/stats/ranking` + `frontend/js/ranking.js` | `analysis/ranking.py`、`frontend/js/ranking.js` | 網頁顯示「最常沒車站 Top 10」表格 |
| 9/9(二) | 單站歷史 | 寫 `/stations/{sno}/history` + `frontend/js/station-detail.js`;點地圖站點 → 顯示 7 天折線 | `app/routers/stations.py`(補)、`frontend/js/station-detail.js` | 點任一站,右側出現該站近 7 天可借車折線 |
| 9/10(三) | 快取 + 整合 | 寫 `app/cache.py`,套用到 stats endpoints。整體串接檢查、手機版樣式 | `app/cache.py` | stats endpoint 第二次呼叫明顯變快;手機開不跑版 |
| 9/11(四) | 進階(擇一) | **通勤潮汐** `analysis/flow.py` + `/stats/commute-flow` + 前端;或 **站點分群** `analysis/cluster.py` + `/stats/clusters` + 前端 | `analysis/flow.py` 或 `analysis/cluster.py` + 對應前端檔 | 有一張圖能講出一個具體洞察 |
| 9/12(五) | 進階收尾 / 緩衝 | 完成進階項目,或當緩衝日補前面進度 | — | 所有已排功能都在線上版可用 |

### 第 3 週:收尾與交件(9/13 六 ～ 9/16 二)

| 日期 | 階段 | 具體工作 | 產出檔案 | 完成標準 |
|---|---|---|---|---|
| 9/13(六) | 文件 | 寫完整 `README.md`(定位、架構圖、畫面截圖、套件、本地執行步驟、已知限制、未來方向)。寫 `analysis.md` 記錄分析結論(潮汐數字、缺車站排行、分群結果) | `README.md`、`analysis.md` | 別人照 README 能把專案跑起來 |
| 9/14(日) | 測試 + CI | 補 `tests/test_store.py`、`tests/test_api.py`;寫 `.github/workflows/ci.yml`(ruff + pytest) | `tests/*`、`.github/workflows/ci.yml` | CI 全綠;`pytest` 全部通過 |
| 9/15(一) | 驗收 | 全流程手動驗證一遍;修剩餘 bug;錄一段 2 分鐘 demo 影片或截圖組;整理 git 歷史 | demo 影片/截圖 | 線上版、repo、文件三者一致 |
| 9/16(二) | 交件緩衝 | 最後檢查、繳交 | — | 完成 |

---

## 十一、風險與降級方案

> 遇到卡關時,從上往下退一階,專案仍然完整可交、可展示。

| 階 | 方案 | 即時地圖 | 歷史累積 | 分析 API |
|---|---|---|---|---|
| 1(目標) | 雲端 Postgres + Actions 排程 + Render API | 前端直打官方 JSON | Actions 每 10 分寫 Postgres | Render 讀 Postgres |
| 2 | 後端只做分析,不碰即時 | 前端直打官方 JSON | Actions 每 10 分寫 Postgres | 同上 |
| 3 | 不用雲端 DB,資料存在 repo | 前端直打官方 JSON | Actions 每 10 分跑 `run_once`,把 SQLite/CSV **commit 回 repo** | API 讀 repo 內的 SQLite(每次部署更新) |
| 4(最低) | 純本地 | 展示時本機開前端直打官方 JSON | 自己電腦跑 `scheduler.py` 累積 | 展示時本機 `uvicorn`,讀本地 SQLite |

**不能省的底線:** 8/27–8/29 一定要讓「抓取 → 寫入 DB」跑起來並開始累積,否則 9/16 沒有足夠歷史資料做分析。

其他風險:

| 風險 | 對策 |
|---|---|
| 官方 API 欄位名與預期不同 | Day 1 先打一次確認;`transform.py` 的 `FIELD_MAP` 支援多種名稱 |
| 官方 JSON 對瀏覽器 CORS 關閉 | 改走後端 `/stations/live` 代理 |
| GitHub Actions 排程延遲 | 可接受;`sample_count` 欄位記錄實際樣本數,分析時用比例而非絕對次數 |
| 免費後端休眠、首次請求慢 | README 說明;或用 cron-job.org 定時 ping `/health` 保活 |
| Postgres 免費容量上限 | 只保留最近 60–90 天 snapshot,更舊的資料靠 station_hourly 保留 |
| 前端圖表資料量太大卡頓 | 一律走後端聚合(station_hourly),前端不處理原始 snapshot |

---

## 十二、面試準備重點

### 12.1 履歷條目(可依應徵方向調整強調點)

> **YouBike 台北市車況即時儀表板** — Python, FastAPI, pandas, SQLAlchemy, PostgreSQL, ECharts, GitHub Actions
> - 設計每 10 分鐘排程的資料管線(GitHub Actions cron),以 idempotent upsert 寫入雲端 PostgreSQL,累積逾百萬列站點快照
> - 以 pandas 建立每小時聚合表,分析通勤潮汐與尖離峰缺車,發現 XX 區早高峰平均 X% 時間無車可借,並定位出最缺車的 10 個站點
> - 以 FastAPI 提供聚合後的分析 API(含 TTL 快取與 Pydantic schema),前端 ECharts 儀表板整合即時地圖、尖離峰熱力圖與單站歷史查詢,已部署上線

### 12.2 展示物件

- **公開 demo 網址**(最重要,勝過截圖)
- GitHub repo,README 有架構圖 + 畫面截圖 + 已知限制 + 未來方向
- `analysis.md`:帶數字的分析結論
- 2 分鐘 demo 影片

### 12.3 可能被問的問題(先準備好答案)

| 問題 | 回答方向 |
|---|---|
| 為什麼用 PostgreSQL 而不是一直用 SQLite? | 本地開發用 SQLite;正式環境需要 Actions 與 API 共用同一份資料,且資料持續增長,故用雲端 Postgres,SQLAlchemy 讓兩者可切換 |
| 排程掛掉怎麼辦?如何知道? | `run_once` 記錄每次抓到的站數;`sample_count` 反映資料完整度;可加失敗通知(Actions 失敗會寄信) |
| 資料量變大、查詢變慢怎麼辦? | 已做每小時聚合表,前端查聚合不查原始;`(sno, ts)` 索引;更舊資料可下採樣或封存 |
| 借還流量是推估的,準確嗎? | 用相鄰快照差值近似,說明限制(調度補貨會造成雜訊、10 分鐘間隔會低估短時進出),報告中誠實標註 |
| 如何驗證清洗結果正確? | 單元測試涵蓋欄位對應與髒資料過濾;抽樣人工比對官方網站 |
| 為什麼即時地圖讓前端直接打官方 API? | 官方是靜態 JSON、更新頻繁,直連即時性最好且不依賴會休眠的後端;後端保留代理版作為備援 |

---

## 附錄:第一天指令備忘

```bash
# 1. 建專案
mkdir youbike-dashboard && cd youbike-dashboard
git init
python -m venv .venv && source .venv/bin/activate
# 建立 requirements.txt 後
pip install -r requirements.txt

# 2. 探測官方 API 真實欄位(重要)
python -c "import requests, json; d = requests.get('https://tcgbusfs.blob.core.windows.net/dotapp/youbike/v2/youbike_immediate.json', timeout=10).json(); print(json.dumps(d[0], ensure_ascii=False, indent=2)); print('站數:', len(d))"

# 3. 建表
python -m db.init_db

# 4. 跑一次抓取
python -m ingest.run_once

# 5. 連跑第二次,確認不重複
python -m ingest.run_once
```
